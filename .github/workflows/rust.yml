name: Rust

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Install LLVM@10
      run: sudo apt install -y llvm-10-dev
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
